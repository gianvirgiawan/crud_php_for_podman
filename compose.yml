version: '3.8'

services:
  # Service untuk database MariaDB
  mariadb-gian:
    build:
      context: .
      dockerfile: mariadb.Containerfile
    container_name: mariadb-gian
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password_123
      MYSQL_DATABASE: db_akademik
      MYSQL_USER: user_gian
      MYSQL_PASSWORD: user_password_123
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

  # Service untuk aplikasi web PHP
  php-apache-gian:
    build:
      context: .
      dockerfile: php-apache.Containerfile
    container_name: php-apache-gian
    restart: always
    ports:
      - "8000:80"
    volumes:
      - ./src:/var/www/html
    environment:
      DB_HOST: mariadb-gian
      DB_NAME: db_akademik
      DB_USER: user_gian
      DB_PASS: user_password_123
      DB_PORT: 3306
    depends_on:
      - mariadb-gian

  # Service untuk PhpMyAdmin
  phpmyadmin-gian:
    # PERBAIKAN: Gunakan nama image yang lengkap untuk Podman
    image: docker.io/phpmyadmin/phpmyadmin
    container_name: phpmyadmin-gian
    restart: always
    ports:
      - "8001:80"
    environment:
      PMA_HOST: mariadb-gian
      PMA_PORT: 3306
    depends_on:
      - mariadb-gian

volumes:
  mariadb_data: